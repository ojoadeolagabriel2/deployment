---
apiVersion: v1
kind: Service
metadata:
  name: {{ project_name }}-service
spec:
  selector:
    app: {{ project_name }}-web
  ports:
    - port: {{ service_port | default('12345') }}
      targetPort: {{ container_port | default('8080') }}
  type: LoadBalancer